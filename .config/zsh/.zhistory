mv .config/gnupg .gnupg
echo $GOPATH
ls
rm -rf .tmux.conf
ls -la .config
rm -rf .config/{Dharkael,Realm\ Studio,chromium,deadbeef,font-manager,icons,inkscape,pavucontrol.ini,okularrc,msmtp,mutt,neofetch,qutebrowser,suspendrc,resumerc,wal,wireshark,yarn,vlc}
echo $PATH
rm -rf .config/xfce4
ls -la
rm -rf .mozilla
sudo vpm remove mozilla
sudo vpm remove firefox
pingrtt
/home/valentin/.config/local/bin/pingrtt
lfcd
ln -s ~/Projekte/dotfiles/.config/tmux ~/.config
tmux -h
type tmux
